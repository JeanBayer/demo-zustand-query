import { DarkModeCheckbox, OrderAction } from "../components";

export const Actions = () => {
  return (
    <div className="actions">
      <OrderAction />
      <DarkModeCheckbox />
    </div>
  );
};
